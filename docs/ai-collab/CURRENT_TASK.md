# Current Task

> 이전 사이클(gold-200 teacher-label 진단)은 `docs/ai-collab/archive/2026-09-07-gold200-diagnosis/`에
> 보관됨. 그 사이클의 `DECISION.md`가 이번 로드맵의 근거.

## 목표(Goal)

`CLAUDE_REVIEW.md`에 제안된 "ML 전환 로드맵"(9단계)을 실행하기 전에, 순서/누락/근거를
비판적으로 검토한다. 특히 사람 라벨링 리소스가 필요한 4개 항목(1, 2, 4, 6)에 실제로
투입할 가치가 있는지, 순서가 맞는지 확인한다.

## 진행 방식 (3단계, 무한 루프 금지)

1. **Claude Code (로드맵 제안, done)** -> `CLAUDE_REVIEW.md`
2. **Codex (비판적 검토)** -> `CODEX_REVIEW.md`
3. **Claude Code (합의/불일치 정리 + 최소 실험 + 결정)** -> `DECISION.md`

각 단계는 **코드를 수정하지 않는다.** 최소 실험이 필요하면 3단계에서 기존 스크립트를
읽기 전용으로 실행해서 근거만 추가한다.

## Round 2 지시 (Codex용)

`CONTEXT.md`와 `CLAUDE_REVIEW.md`를 읽고, 9단계 로드맵에서:

- 순서가 잘못된 부분 (선행되어야 할 게 뒤에 있거나 반대)
- 근거가 약한 우선순위 판단
- 빠진 단계 (특히 평가 방법론 관점에서)
- "사람 라벨링 필요/불필요" 분류가 틀린 항목
- 실행 규모(몇 개 발화, 몇 명 검수 등)를 더 구체화해야 하는 부분

을 찾아서 `CODEX_REVIEW.md`에 작성한다. 아직 코드는 수정하지 않는다.
